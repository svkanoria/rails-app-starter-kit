<p><%= t('devise.mailer.hello') %> <%= @resource.email %>!</p>

<p><%= t('.instructions') %></p>

<p>
  <%= link_to t('.unlock_my_account'),
              unlock_url(@resource, unlock_token: @token,
                         subdomain: @resource.tenant.subdomain,
                         locale: I18n.locale) %>
</p>
