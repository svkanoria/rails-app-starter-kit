<%#
  # Server-side flash messages.
  # Really only used for the devise workflow, because devise views are the only
  # server-generated views. We must ignore the :timedout key, as devise uses it
  # for keeping internal state. See
  # https://github.com/plataformatec/devise/issues/1777.
  #%>
<script type="text/javascript">
  var ServerFlash = <%= raw flash.to_hash.except(:timedout).to_json %>;
</script>
